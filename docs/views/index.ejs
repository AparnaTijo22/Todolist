<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="style.css">
    <title>TodoList</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.10.2/css/all.css">
</head>
<body>

    <h2><%= today %>, <%= month %> <%= date %></h2>

    <div class="box">
        
            <ul>
                <form action="/delete" method="post">
                    <div class="itemBox">
                        <% for(let i=0; i<items.length; i++){ %> 

                            <li class="itemList" id="itemLi"> 
                                <input type="checkbox" name="checkbox" value="<%= items[i]._id %>" id="myCheckBox" onchange="this.form.submit()"><p id="itemPara"> <%= items[i].name %></p> </li>
                            <% } %>
                    </div>
                </form>

                <form action="/" method="post">
                <li class="itemList"><input type="text" id="addedItem" name="newItem" placeholder="New Item"> <input type="submit" value="+" class="bttn"></li>
            </ul>
        </form>
    </div>
</body>
</html>